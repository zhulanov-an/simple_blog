<h1>New contact</h1>
<%= form_for :contact, url: contacts_path do |f| %>
  <p>
    <%= f.label :email %>
    <%= f.email_field :email %>
  </p>
  <p>
    <%= f.label :message %>
    <%= f.text_area :message %>
  </p>
    <p>
    <%= f.submit 'Send message'%>
  </p>
<% end %>